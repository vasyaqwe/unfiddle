{
   "name": "@ledgerblocks/api",
   "scripts": {
      "dev": "wrangler dev --local src/index.ts --port 8787",
      "db:migrate": "wrangler d1 migrations apply ledgerblocks --local",
      "deploy": "wrangler deploy --minify src/index.ts --env production",
      "typecheck": "tsc --noEmit"
   },
   "dependencies": {
      "@ledgerblocks/infra": "workspace:*",
      "@ledgerblocks/core": "workspace:*"
   },
   "devDependencies": {
      "@ledgerblocks/tsconfig": "workspace:*",
      "@types/bun": "latest",
      "wrangler": "^4.12.0"
   }
}
